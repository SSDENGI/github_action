name: 05-2-filters-activity-types
on:
  pull_request:
    types: [closed]
    branches:
      - main
jobs:
  echo:
    runs-on: ubuntu-latest
    steps: 
      - name: echo when the pr is closed
        run: echo "Running whenever a PR is closed AND base branch is main"