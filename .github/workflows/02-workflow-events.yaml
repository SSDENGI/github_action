name: 02 - Workflow Events
on: 
  push:

  workflow_dispatch:

  schedule:
    - cron: '* * * * *'
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: show the github context
        run: echo "${{toJson(github)}}"
      - name: show the event type
        run: echo "Event Triggering the Workflow is : ${{github.event_name}}"
