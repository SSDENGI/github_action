name: 09 - Using Functions
on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
  workflow_dispatch:

jobs:
  echo1:
    runs-on: ubuntu-latest
    steps:
      - name: Failing step
        run: |
          exit 1
      - name: I will be skipped
        if: ${{ sucess()}}
        run: |
          echo " I will print if previous step is successful "
      - namne: I will execute
        if: ${{ failure()}}
        run: |
          echo " I will print if previous step failed "

      - name: i Will Definately execute
        if: ${{ always()}}
        run: |
          echo "I will always print, except when the workflow is cancelled"

      - name: I will execute when cancelled
        if: ${{ cancelled()}}
        run: |
          echo "I will print if the workflow is cancelled"
